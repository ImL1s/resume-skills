Permission allow rule (../../../.claude/settings.json): Write(/**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(/**) instead (Edit rules cover all file-editing tools).
Permission allow rule (../../../.claude/settings.json): Write(~/**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(~/**) instead (Edit rules cover all file-editing tools).
Permission allow rule (../../../.claude/settings.json): Write(./**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(./**) instead (Edit rules cover all file-editing tools).
Permission allow rule (../../../.claude/settings.json): Write(~/.claude/**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(~/.claude/**) instead (Edit rules cover all file-editing tools).
Permission allow rule (../../../.claude/settings.json): Write(~/Documents/**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(~/Documents/**) instead (Edit rules cover all file-editing tools).
Permission allow rule (../../../.claude/settings.json): Write(/tmp/**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(/tmp/**) instead (Edit rules cover all file-editing tools).
Permission allow rule (../../../.claude/settings.json): Write(/private/**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(/private/**) instead (Edit rules cover all file-editing tools).
報告已寫入 `/Users/iml1s/Documents/mine/resume-skills/.omc/research/dual-review-fable.md`（覆寫先前的 BLOCKED placeholder）。以下為完整報告內容（stdout）：

---

# Dual-Review 報告 — Claude Fable 5 seat（portable-resume-skills V1 全樹）

- 審查者：Claude Fable 5（獨立 read-only review seat）
- 日期：2026-07-20
- 範圍：`/Users/iml1s/Documents/mine/resume-skills` working tree（main、無 commit）；依 `.omc/research/dual-review-brief-safe.md` 八個維度全覆蓋
- 方式：全量閱讀 `src/portable_resume/**`（含六個 adapters、installer、resources 模板）、`tests/**`（unit/adapters/security/integration/e2e）、`scripts/*`、`docs/*`、PRD 與 ultragoal 驗證文件；實際執行 brief 指定的全部驗證指令，另外自行做了三組對抗性實機驗證（installer 生命週期、drifted-owned reinstall、uninstall 殘留）。

---

## Verdict: **APPROVE WITH MINOR FIXES**

決定性（deterministic）V1 bar 誠實達成：145 個測試全綠、36-cell packaging matrix 正確且 live 欄位誠實標 `not-run`、六個 adapter 的 fail-closed 紀律一致、request-v1 邊界與 handoff 引用防護經對抗性測試證明有效、clean-room 文件與掃描到位。未發現 Critical 級安全缺陷。

但我以實機驗證找到 **3 個 Important**（installer 強健性與 PRD 規範文字的落差，其中一項會無備份地銷毀使用者對 owned 檔案的本地修改）與若干 Minor。這些不推翻目前「deterministic bar pass、live/dual-OS 誠實未宣稱」的狀態（release 本來就被 live smoke 與 AC-18 擋住），但應在正式 release claim 引用 AC-12 / PRD §7.3 之前修復。

---

## Critical（release claim 前必修）

**無。**

未發現：source CLI 被喚起的路徑、source store 被改寫的路徑、recovered content 逃出 blockquote/inert 邊界的路徑、path/symlink escape、SQLite 直連 live 資料庫、installer 覆寫非 owned 檔案（未 force）的路徑。

---

## Important（應儘快修）

### I-1. Installer journal/manifest 完全沒有 fsync，弱於 PRD §7.3 的規範性要求
- 位置：`src/portable_resume/install/transaction.py` — `_write_journal()`（L371-377）、`execute_install()` commit 迴圈與 manifest 寫入（L336-358）。
- PRD §7.3 明文要求「write and **fsync** a durable journal … replace owned paths one at a time with **directory fsync**」。實作中 journal 用 `Path.write_text` + `os.replace`（無 file/dir fsync）、staged 檔案用 `Path.write_bytes`（無 fsync）、**manifest（commit marker）直接 `write_text` 覆寫，既非 atomic replace 也無 fsync**。
- 後果：process crash 情境（測試有覆蓋）可偵測、可恢復——這部分成立；但 power-loss 情境下 journal 可能整份消失或 manifest 半截，「durable journal 讓 mixed state 可偵測」這個 AC-12 的宣稱在 power-loss 下不成立。
- 建議：journal 與 manifest 一律 temp + fsync(file) + `os.replace` + fsync(parent dir)；staged 檔在 commit 前 fsync。對照組：`snapshot.py` 的私有 SQLite 副本反而有做 `os.fsync`（L344）。

### I-2. `recover_root()` 對未完成 journal 的語意與 PRD／自身註解都不符：無條件刪 journal、不做完成/失敗封閉
- 位置：`transaction.py` L410-447。
- PRD §7.3：「the next `recover` … **idempotently completes or restores** each recorded path … **unrecoverable drift fails closed**」。程式碼 L423 註解也寫「keep journal if unrecoverable drift remains」。
- 實際行為：incomplete journal 只還原 sandboxed backups，**不會**用 journal 記錄的 staged hash 完成或校驗已 committed 的路徑，然後**無條件 `os.remove(path)` 刪除 journal**（L446）。若 crash 發生在部分 commit 之後（無 backups 的常見情況），recover 回報 `restored_paths: 0`、清掉 journal、解除 `E_RECOVERY_REQUIRED` 封鎖，磁碟上留著新舊混合檔案，只剩事後 `verify` 才會以 `E_VERIFY_MISMATCH` 發現。
- 建議：recover 時逐路徑比對 journal 記錄的 sha256——已 committed 且 hash 相符者視為完成；不符且無 backup 者保留 journal 並回報 fail-closed；全部收斂才刪 journal。

### I-3. Reinstall 會無備份、無警告地覆寫使用者已修改的 owned 檔案（實機驗證重現）
- 位置：`transaction.py` `plan_install()` L198-202（`owned_shared = existing is not None and rel in existing.files` → 直接列入 `replaces`）、`_classify_dest()` L264-266。
- 實測：install claude → 對 `resume-grok/SKILL.md` 追加一行本地修改 → 再次 install（未加 `--force-with-backup`）→ exit 0，修改**直接消失**，`backups/` 不存在，plan 顯示 `replaces: ['resume-grok/SKILL.md'], backups: []`。
- 不一致點：同一種 drift，`uninstall` 以 `retained_drift` 保留、`verify` fail-closed（實測 exit 7），唯獨 reinstall 靜默銷毀。專案自己的核心倫理是「refusal by default、force 才 backup」。
- 建議：將「owned 但磁碟 hash ≠ manifest 記錄 hash」歸類為 replace+backup（自動寫入 `.portable-resume/backups/`）並在結果 JSON 明示。附帶：`plan_install` 的 `owned` 變數被 `owned_shared` 完全吸收，是死碼。

---

## Minor / nits

1. **Uninstall 殘留空目錄（實機驗證重現）**：乾淨 install→uninstall 後留下 11 個目錄（六個空的 `resume-*`、`.portable-resume/runtime/...` 鏈）與 `install.lock`。根因是 `_cleanup_empty_dirs()`（transaction.py L546-554）bottom-up walk 中父目錄的 `dirnames` 是走訪當下快照，子目錄剛被 rmdir 後父目錄仍以為非空而跳過。修法：無條件 `try: os.rmdir` 或以 `os.listdir` 重查。
2. **`codex.py` 用 `getattr(subprocess, "Popen")` 繞過自家靜態掃描**：`test_isolation.py` 逐字串斷言 `subprocess.Popen(` 不存在於 src，而 zstd decoder（audited 例外）刻意用 getattr 規避。語意仍成立，但此靜態閘門對「未來有人如法炮製」零防護。建議改為顯式 allowlist 並註明 audit 依據。
3. **request-file 模式的「fixed argv」是 prompt-enforced、非 machine-enforced**：`--request-file` 模式仍接受 `--source-root`（生效）、`--format json`（改變輸出）、`--within-min`（靜默忽略）。風險有限（輸出仍 inert/sanitized），但建議至少拒絕 `--within-min` 並在文件記錄此測試縫隙。
4. **`--host all` 中途 conflict 丟棄已完成結果的輸出**：任一 host 拋 `E_INSTALL_CONFLICT`（如 project scope codex+antigravity 共 root）即整體以 diagnostic 結束，先前已成功的 host 安裝了卻無成功輸出。建議逐 host 收集成敗並列。
5. **Attestation 文件範圍不一致**：`clean-room-attestation.md` 明寫僅限 **G001**，`provenance.md` 卻涵蓋 **G001–G004**，應同步。
6. **`render_session()` 在接近 8 MiB 的合法 session 上 fail-closed（exit 7）**：`--format json` 成功、預設 handoff 因引用前綴超限失敗。方向正確，但值得文件化或預留 render overhead。
7. **`.omc/` 未列入 `.gitignore`**（`.omx/` 有）：首次 commit 前建議補上。
8. Nits：`cli.py` recover 的 `as_json=... or True` 恆真；`execute_install` 的 `generation != 0` 分支不可達；`RootLock` 在 Windows 是 no-op（已文件化非 V1 live 目標）；`_ANSI` regex 未涵蓋單字元 escape，但後續 C0 過濾使其不可利用。

---

## 指令驗證結果（實際執行，未跳過）

| 指令 | 結果 |
|---|---|
| `python3 -m compileall -q src scripts tests` | **exit 0** |
| `PYTHONPATH=src python3 -m unittest discover -s tests -q` | **exit 0 — `Ran 145 tests … OK`**（3.29s） |
| `scripts/portable-resume --help` | **exit 0**；正好六個 source key + `list\|show` |
| `scripts/portable-resume self-check --json` | **exit 0**；`"ok":true`、`"matrix":{"cell_count":36,"expected":36,"ok":true}` |
| `scripts/install-resume-skills matrix --json` | **exit 0**；`packaging_cells_supported:36`、**`live_cells_supported:0`、每格 `live_evidence:"not-run"`** |

追加對抗性實機驗證（temp HOME/project）：

| 驗證 | 結果 |
|---|---|
| `install --host all --scope global --dry-run`（空 temp HOME） | exit 0；6 結果全 ok+dry_run；**HOME 內 0 檔案建立**（純度成立） |
| install → verify（claude project） | 均 exit 0 |
| drift 後 verify | **exit 7、`E_VERIFY_MISMATCH`**（fail-closed 成立） |
| installed `run_reader.py` + request-v1 + 惡意 `--expected-source codex` | exit 0；SECURITY BOUNDARY handoff；wrapper 正確剝除覆寫 hard-bind 回 claude |
| drifted-owned reinstall（無 force） | **exit 0 但本地修改被無備份覆寫**（→ I-3） |
| 乾淨 uninstall 後盤點 | 42 檔案移除；**殘留 11 空目錄 + install.lock**（→ Minor 1） |
| 全部操作後 `git status tests/fixtures` | 無任何內容變動——fixture bytes/mtimes 未被改動 |

---

## 各維度審查摘要

1. **安全不變量** — ✅ 穩固。runtime 唯一 subprocess call site 是 codex zstd decoder（固定絕對路徑 allowlist、空 PATH、no shell、timeout、16 MiB 上限）；`sqlite3.connect` 僅存在於私有副本（`mode=ro&cache=private`、`uri=True`、query_only 回讀斷言）；hot journal fail-closed、`-shm` monitored-not-copied 有專測；`O_NOFOLLOW` descriptor-relative 開檔 + 三讀一致 + parent membership 指紋抓 same-stat spoof；recovered content 全鏈 inert，hostile 注入（換行/OSC-8/RTL-override）無法產生未引用行。
2. **合約正確性** — ✅。exit code 對映、`--json` 互斥、預設格式、diagnostics 固定訊息表、candidates 閉合 schema+排序、8 MiB aggregate 雙軌驗證、handoff 無時鐘 byte-identical 皆有測試。
3. **六個 adapters** — ✅。fixtures：claude 7 / codex 12 / cursor 8 / opencode 7 / antigravity 6 / grok 6，每源含 unsupported/corrupt/busy/immutability/injection；未知 schema 一律 fail-closed 不猜；Grok 對 rewind/compaction 事件 fail-closed 是正確保守選擇。
4. **Packaging 6×6** — ✅。36 格、strict frontmatter 雙處強制、run_reader hard-bind source 且剝除覆寫（e2e subprocess 實證）、共 root 衝突誠實回報。
5. **Installer** — ⚠️ 大致成立，見 I-1/I-2/I-3 與 Minor 1/4。
6. **Evidence honesty** — ✅ 出色。live 全 `not-run`、AC-18 僅宣稱本機 OS、independent review 明文 deferred 且拒發明 APPROVE/CLEAR；本報告即該 deferred review 的其中一席。
7. **Clean-room** — ✅（attestation-based）。src 無禁止參照（自動掃描）、NOTICE/LICENSE/provenance 齊備、fixtures 全 synthetic；`.omx/tmp/host-probes` 的 vendor 副本已被 gitignore、不含禁止路徑內容，維持不 commit 即可。
8. **測試品質** — ✅ 高。無 skip/stub/theater；security 測試以 PATH-shim、monkeypatch、same-stat spoof、hostile metadata 驅動 **shipped** 入口；e2e 卸除 PYTHONPATH 證明 installed runtime 自行解析。唯一保留意見是 Minor 2。

---

## Residual risks / 誠實不宣稱

1. **36 格全部 `verified-filesystem`、0 格 live**：AC-10 的 installed-version-proven 尚未達成，V1 release 仍被此項阻擋——現有文件已正確如此陳述，本報告確認屬實。
2. **AC-18 dual-OS**：僅 macOS 有綠色證據；Linux 誠實未宣稱。
3. **Grok 優先權（AC-13）**：僅 filesystem policy 落實；live 優先權證明未跑，與文件一致。
4. **Clean-room 為 attestation-based**：機器只能證明 shipped tree，無法證明過程；且有 Minor 5 的範圍不一致待補。
5. **秘密 redaction 是 best-effort**，非完整 DLP（README 已聲明）。
6. **Power-loss durability**：I-1 修復前，crash-journal 可偵測性只對 process-crash 成立。
7. 本審查為單模型 seat；應與 Codex seat 結論交叉比對後再合併裁決。

---

*報告完。Verdict: **APPROVE WITH MINOR FIXES** — deterministic V1 bar 誠實達成；I-1/I-2/I-3 應在正式 release claim 前修復；live 36-cell 與 Linux runner 證據仍是 release 的既知硬閘門。*
FABLE_DONE
